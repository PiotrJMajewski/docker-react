language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t piotrjmakewski/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true piotrjmakewski/docker-react npm run test